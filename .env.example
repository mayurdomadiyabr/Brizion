# Public canonical site URL — used for OG, sitemap, JSON-LD, llms.txt
NEXT_PUBLIC_SITE_URL=https://brizion.com

# Stripe — server-side (NEVER ship the secret key to the browser)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Stripe — public, safe to expose to the browser if you ever use Elements
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
